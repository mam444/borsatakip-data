# Production: point to a backend proxy that stores the provider key server-side.
EXPO_PUBLIC_MARKET_DATA_PROXY_URL=

# Local development only. EXPO_PUBLIC values are visible in the client bundle.
# Never ship or commit a paid production key here.
EXPO_PUBLIC_TWELVE_DATA_API_KEY=

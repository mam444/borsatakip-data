import React from 'react';

// Google Mobile Ads is native-only. Keeping a web-specific implementation
// lets the full product run in browsers without reserving an empty ad slot.
export function AdBanner(_props: { style?: object }) {
  return null;
}
